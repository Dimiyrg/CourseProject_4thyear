import React from 'react';
import '../App.css';

function Newpage(props) {
    return(
        <header > 
        if({props.name}=='Witcher')
        {
            
        }
        </header>
    )
}
export default Newpage;